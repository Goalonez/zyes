# TDD profile

仅在用户明确要求 TDD、ticket 声明 test-first，或根因不清晰需要可重复反馈信号时读取。

- 先写一个通过公共 interface 观察行为的失败测试，并实际运行以记录 red 证据。
- 期望值来自 spec、已知样例或独立真相，不能由待测实现计算；优先真实依赖，只在必要边界 mock。
- 写最小实现使测试转绿，再在测试持续通过时重构；一次推进一个狭窄垂直切片。
- 同时记录实际 red、green 命令与结果。无法建立可信测试时说明限制并选择其他可重复验证，不伪造 TDD 过程。
