# Zyes 协议恢复

仅在 `scripts/zyes.py` 因运行环境缺少 Python 或脚本文件不可访问而无法执行时读取本文件。命令已经执行但报告状态、格式、路径或权限错误时，保留错误并停止；不要使用手工流程绕过校验。

## 恢复资料

按当前阶段完整读取：

- [STORAGE.md](STORAGE.md)：项目根目录、写入边界和受控块。
- [TASKS.md](TASKS.md)：任务状态、ticket 状态、模板和迁移。
- [DOMAIN.md](DOMAIN.md)：仅在阶段需要 glossary 或 ADR 时读取。

手工操作必须达到与协议工具相同的前置检查、单一状态来源、路径规范化、同项目互斥、失败回滚和写后复读要求。`task.md` 是任务阶段的唯一来源，`result.md` 不含独立 status。无法证明转换安全时停止并报告“未经协议工具验证”，不要猜测性修复多个状态源。

恢复完成后只要脚本重新可用，立即运行对应的 `context` 或 `validate` 命令确认状态；验证通过前不推进下一阶段。
